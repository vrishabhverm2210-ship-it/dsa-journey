1class Solution {
2public:
3bool allmatched(vector<int>&having,vector<int>&find){
4   for(int i=0;i<256;i++){
5    if(having[i]>find[i])return false;
6   }
7   return true;
8}
9    string minWindow(string s, string t) {
10        int n=s.size();
11        int m=t.size();
12        // vectors for storing
13        vector<int>having(256,0);  // for t 
14        vector<int>find(256,0);  // for s
15        // store elements of t into the [having]
16        for(int i=0;i<t.size();i++){
17           having[t[i]]++;
18        }
19        // let's apply minimum window template in string s
20        int high=0;
21        int low=0;
22        int count=INT_MAX;
23          int start;
24        for(high=0;high<n;high++){
25            find[s[high]]++;
26          int len;
27        
28            // right case which we have to pick
29            while(allmatched(having,find)){
30                len=high-low+1;
31             if(len<count){   //MINI case
32             count=len;
33             start=low;
34             }
35                find[s[low]]--;
36                low++;
37            } 
38             
39        }
40
41        if(count == INT_MAX)   // this must be done otherwise it is throwing tle
42    return "";
43return s.substr(start,count);
44
45    }
46};