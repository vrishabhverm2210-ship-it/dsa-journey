1class Solution {
2public:
3bool found(vector<int>&need ,vector<int> & have ){
4    for(int i=0;i<256;i++){
5        if(have[i]<need[i])return false;
6    }
7    return true;
8}
9    string minWindow(string s, string t) {
10        int high=0;
11        int low=0;
12        vector<int>need(256,0);
13        for(int i=0;i<t.size();i++){
14            need[t[i]]++;
15        }
16        vector<int>have(256,0);
17        int count=INT_MAX;
18        int startindex;
19        for(high=0;high<s.size();high++){
20            have[s[high]]++;
21            // right block means , if we found everthing
22            while(found(need,have)){
23                int len=high-low+1;
24                if(count>len){      /// important step
25                    count=len;
26                    startindex=low;
27                }
28                have[s[low]]--;
29                    low++;
30            }
31        }
32        if(count==INT_MAX)return "";
33     return s.substr(startindex,count);
34    }
35};