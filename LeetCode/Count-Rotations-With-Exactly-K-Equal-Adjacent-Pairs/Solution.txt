1class Solution {
2public:
3    int countRotations(string s, int k) {
4        int n=s.size();
5        int len=s.size();
6        string temp=s;
7        int count=0;
8        int score=0;
9        while(n--){
10            // CHECKING LOGIC 
11            int score=0;
12            for(int i=0;i<len-1;i++){
13                if(temp[i]==temp[i+1])score++;
14            }
15            if(score==k)count++;
16            // now logic for cyclic rotation
17            temp=temp.substr(1)+temp.substr(0,1);    
18           
19        }
20        return count;
21    }
22};