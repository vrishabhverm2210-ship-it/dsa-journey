1class Solution {
2public:
3    long long maxSum(vector<int>& nums, int k, int mul) {
4          // LET US USE PRIORITY QUEUE
5        priority_queue<int,vector<int>,greater<int>>pq;// min heap
6       
7        for(int i=0;i<nums.size();i++){
8            if(pq.size()<k){
9                pq.push(nums[i]);
10                continue;
11            }
12            pq.push(nums[i]);
13            pq.pop();
14        }
15        // extract the heap(opposite order)
16        vector<int> v;
17
18         while(!pq.empty()){
19          v.push_back(pq.top());
20           pq.pop();
21       }
22        // for descening order sort it
23        sort(v.rbegin(),v.rend());
24        // now according to ques nikalo bahar(idsd part mai think krna hai)
25         long long sum=0;
26       for(int i=0 ;i<v.size();i++){
27        if(mul>1){
28            sum+=1ll*mul*v[i];
29        }
30           else{
31        sum += v[i];
32           } 
33           mul--;
34  }
35        return sum;
36    }
37};